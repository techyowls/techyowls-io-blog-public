spring:
  application:
    name: langchain4j-guide

langchain4j:
  open-ai:
    api-key: ${OPENAI_API_KEY}
    chat-model:
      model-name: gpt-4o
      temperature: 0.7
      max-tokens: 1000

server:
  port: 8080
